import { firebase, storage } from './Firebase'
import {
  deleteData,
  addData,
  setData,
  firestore,
  getCollectionRef,
  getTimestamp
} from './Firestore'

export {
  firebase,
  storage,
  setData,
  firestore,
  getCollectionRef,
  deleteData,
  addData,
  getTimestamp
}
