import { firebase } from './Firebase'

import {
  setData,
  getData,
  firestore,
  getCollectionRef,
  getDataWithFilter,
  getOrderedData
} from './Firestore'

export {
  firebase,
  setData,
  getData,
  firestore,
  getCollectionRef,
  getDataWithFilter,
  getOrderedData
}
